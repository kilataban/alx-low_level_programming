#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 * @a: number being tested
 * Return: (0) Always means success
 */

int _abs(int a)
{
        if (a < 0)
                return (-a);
        else
                return (a);

        return (0);
}
